---
- name: Ping localhost
  hosts: localhost
  tasks:
    - name: Ping test
      ansible.builtin.ping:

    - debug:
        var: "{{ zsh_powerlevel10k_instant_prompt }}"
#
# - hosts:
#     - workstations
#     - localhost
#   roles:
#     - ansible-role-backup
#     - ansible-role-zsh
#     - ansible-role-workstation

